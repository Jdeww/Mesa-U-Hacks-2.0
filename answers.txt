Answer sheet

1. b) A  
(Root node A has children, making it an internal node.)

2. d) Siblings  
(Children of the same parent node are siblings.)

3. Data in a tree is arranged hierarchically on multiple levels, not sequentially.  
(This is why a tree is non-linear.)

4. c) A node and all its descendant nodes  
(A subtree of a node X consists of that node and all nodes descending from it.)